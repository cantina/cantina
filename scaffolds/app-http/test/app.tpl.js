/**
 * Tests for {{title}}.
 */

var assert = require('assert'),
    {{name}} = require('../');

describe('{{title}}', function() {

  it('should do something', function(done) {
    done();
  });

});
